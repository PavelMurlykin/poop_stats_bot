Мы подготовили файлы `README.md` и `requirements.txt` для вашего бота. README содержит полное описание функциональности, инструкцию по установке и запуску, а также примеры использования.

---

## 📄 README.md

```markdown
# Poop Stats Bot — Telegram бот для отслеживания связи питания и стула

Бот помогает вести дневник питания и оценивать качество стула по Бристольской шкале.  
Вы устанавливаете удобное время для вопросов о завтраке, обеде, ужине и туалете.  
Бот присылает напоминания и сохраняет ваши ответы.  
Анализ данных позволяет увидеть, как те или иные продукты влияют на пищеварение.

## Основные возможности

- ⏰ **Настраиваемое время опросов** — отдельно для завтрака, обеда, ужина и туалета.
- 🍳 **Ввод блюд** — текстовые ответы на вопросы «Что вы ели?».
- 🚽 **Оценка стула по Бристольской шкале (0–7)**:
  - `0` — отсутствие дефекации
  - `1–7` — типы стула (подробности по команде `/bristol`)
- 📊 **Встроенная Бристольская шкала** — команда `/bristol` выводит описания всех типов.
- 📋 **Удобное меню с кнопками** — все настройки через интерактивные кнопки, кнопка «Назад» для возврата.
- 🔄 **Планировщик уведомлений** — бот сам присылает вопросы в установленное время.
- 💾 **Хранение данных в SQLite** — вся история сохраняется локально.

## Команды и меню

### Команды
- `/start` — регистрация и главное меню.
- `/menu` — показать главное меню.
- `/help` — краткая справка.
- `/cancel` — отменить ожидаемый вопрос или ввод времени.
- `/bristol` — показать Бристольскую шкалу.
- `/set_breakfast HH:MM`, `/set_lunch HH:MM`, `/set_dinner HH:MM`, `/set_toilet HH:MM` — установка времени вручную (альтернатива меню).

### Меню (интерактивные кнопки)
- 🍳 Завтрак — установить время завтрака.
- 🍲 Обед — установить время обеда.
- 🍽️ Ужин — установить время ужина.
- 🚽 Туалет — установить время туалета.
- ⏰ Мои настройки — посмотреть текущие времена.
- 📊 Бристольская шкала — показать шкалу.
- ❓ Помощь — показать помощь.
- ◀ Назад — возврат в главное меню (на экранах настроек, шкалы, помощи).

## Требования

- Python 3.7 или выше
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

## Установка и запуск

1. **Клонируйте репозиторий** (или создайте папку с файлами):
   ```bash
   git clone <URL>
   cd poop_stats_bot
   ```

2. **Создайте и активируйте виртуальное окружение** (рекомендуется):
   ```bash
   python -m venv venv
   source venv/bin/activate      # Linux/Mac
   venv\Scripts\activate         # Windows
   ```

3. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройте токен бота**:
   - Создайте файл `.env` в корневой папке.
   - Добавьте строку:
     ```
     TELEGRAM_TOKEN=ваш_токен_бота
     ```

5. **Запустите бота**:
   ```bash
   python poop_stats_bot.py
   ```
   Если всё правильно, вы увидите сообщение `Бот запущен...`

## Использование

1. Отправьте боту команду `/start`.
2. В главном меню нажмите, например, «🍳 Завтрак».
3. Введите время в формате `ЧЧ:ММ` (например, `08:00`). Бот подтвердит сохранение.
4. Аналогично настройте остальные приёмы пищи и туалет.
5. В указанное время бот пришлёт вопрос:
   - «🍳 Что вы ели на завтрак?» — ответьте текстом.
   - «🚽 Оцените качество стула за вчерашний день…» — введите число от 0 до 7.
6. Чтобы посмотреть текущие настройки, нажмите «⏰ Мои настройки».
7. Если хотите отменить ожидание (например, случайно нажали кнопку), используйте `/cancel`.

## Структура базы данных (SQLite)

- **users** — пользователи и их времена опросов.
- **meals** — записи о приёмах пищи (дата, завтрак, обед, ужин).
- **stools** — оценки стула (дата, качество 0–7).
- **notifications_log** — лог отправленных уведомлений (чтобы не дублировать).
- **bristol_scale** — справочник Бристольской шкалы (заполняется автоматически).

Данные связываются по датам: стул за сегодня относится к еде за вчера.

## Пример работы

```
Пользователь: /start
Бот: 👋 Привет! ... (показывает меню)

Пользователь нажимает «🍳 Завтрак»
Бот: Введите время завтрака в формате ЧЧ:ММ (например, 08:00):
Пользователь: 07:30
Бот: ✅ Время завтрака установлено на 07:30.

В 7:30 бот присылает:
🍳 Что вы ели на завтрак?
Пользователь: Овсянка с бананом и кофе
Бот: ✅ Информация о breakfast сохранена.

На следующий день в 9:00 (время туалета) бот спрашивает:
🚽 Оцените качество стула за вчерашний день (0–7)...
Пользователь: 4
Бот: ✅ Оценка стула сохранена.
```

## Лицензия

Этот проект распространяется под лицензией MIT. Вы можете свободно использовать, модифицировать и распространять его.
```